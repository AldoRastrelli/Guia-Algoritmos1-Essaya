#include <stdio.h>

void ord_seleccion(int lista[], int long_lista);
int buscar_posicion_maximo(int lista[], int long_lista);

int main(){

    int lista[] = {10,1,-2,3};
    int long_lista = 4;
    printf("%d,%d,%d,%d\n",lista[0],lista[1],lista[2],lista[3]);
    ord_seleccion(lista,4);
    printf("%d,%d,%d,%d\n",lista[0],lista[1],lista[2],lista[3]);
}

void ord_seleccion(int lista[], int long_lista){

    for (int i = 1; i >1, )
        int indice, int maximo = buscar_posicion_y_maximo(lista,long_lista-i)
}

int buscar_posicion_y_maximo(int lista[], int long_lista){

    for ( int i = 0; i < long_lista; i++){

        int maximo = lista[0];
        int indice = i;
        if (maximo < lista[i]){
            maximo = lista[i]
            indice = i
        }
    }return indice, maximo;

}

        

